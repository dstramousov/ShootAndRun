# Font resources

Runtime font assets live here.

The default runtime UI configuration expects:

```text
data/fonts/PressStart2P-Regular.ttf
```

If the file is missing or fails to load, the runtime logs a warning and falls
back to the default raylib font.
